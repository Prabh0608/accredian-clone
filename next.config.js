/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enables App Router by default in Next.js 13+
  reactStrictMode: true,
  // Add any custom config here
};

export default nextConfig;
